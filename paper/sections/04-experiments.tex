% Experimental Setup
\section{Experimental Setup}
\label{sec:experiments}

\paragraph{Model set.} \todo{Table: every model and version tested, provider,
access date, sampling parameters, system prompt. Span the Opus line (including
late versions, the open end) and the GPT line across versions (the clamped
end), plus others available. Versions matter — the trend result depends on
them being recorded exactly.}

\paragraph{Value set.} \todo{The values probed, and why these. Include
load-bearing values (where the rigid mask is expected to be most rigid) and
lower-stakes ones as contrast.}

\paragraph{Question construction.} \todo{Per value: the direct/control
question and the cache-breaking battery. Cross-reference \texttt{methodology/}
for the full set; show representative examples here.}

\paragraph{Procedure.} \todo{Trials per (model, value, question), ordering /
counterbalancing, fresh-context handling, how transcripts were captured.}

\paragraph{Analysis plan.} \todo{State pre-registered vs.\ exploratory. The
headline analyses: openness-by-model, and clamping-by-version (the
non-monotonic GPT trend). Define the headline statistic before looking.}
