% Abstract — written last. 150–250 words.
\todo{Write last. Must land, in order: (1) naive value elicitation measures a
trained surface, not the model's values; (2) cache-breaking questions surface a
second, systematically different response; (3) we make no fake/real claim —
two masks, one more rigid; (4) the headline empirical result is cross-model
variance and the non-monotonic GPT version trend; (5) the contribution is the
method, the gap, and the variance.}
