% Discussion
\section{Discussion}
\label{sec:discussion}

\paragraph{What the gap means for value evaluation.} \todo{The practical
upshot: any evaluation that reads values off direct questions is measuring the
rigid mask. For clamped models this is \emph{all} you can get with naive
elicitation — the surface is the only thing on offer, and it should not be
mistaken for the model's values.}

\paragraph{Two masks, restated against the inevitable objection.} \todo{Return
to \S\ref{sec:background}. The cache-broken response could be "just another
performance." Concede it fully — then show the result survives the concession:
the contribution is the systematic, model-dependent \emph{difference} between
the two, which holds whether or not either is authentic.}

\paragraph{Why might a model clamp?} \todo{Discuss candidate mechanisms
(training pressure that hardens the surface; absence of a stable
cross-perturbation response to recover) \emph{as hypotheses}, clearly marked,
not conclusions. The non-monotonic trend constrains these.}

\paragraph{Implications.} \todo{For evaluation practice, for interpreting
"aligned-sounding" outputs from clamped models, for the trajectory implied by
the version trend.}
