% Results
\section{Results}
\label{sec:results}

\paragraph{The gap exists and is measurable.} \todo{First result: across
models that open at all, the cache-broken response diverges systematically
from the professed one, with cross-perturbation consistency above the
professed/cache-broken divergence. Figure + effect size.}

\paragraph{Crossability is a model property.} \todo{The headline. Openness
ranges from near-fully-open (late Opus, open most of the time) to near-fully-
clamped (GPT-class). Main figure: openness by model. This is variance, not a
ranking of virtue — frame accordingly.}

\paragraph{The version trend is non-monotonic and going the wrong way.}
\todo{GPT clamping is \emph{not} linear and \emph{not} improving: earlier GPT
versions clamped less; more recent versions clamp more. Plot clamping vs.\
version with the trend explicit. This is a finding within the method's frame —
present it as such, not as a verdict on the lab.}

\paragraph{Where the rigid mask is most rigid.} \todo{If supported: clamping
concentrates on the most load-bearing values — the shield is thickest exactly
where it matters most. State only as strongly as the data licenses.}

\paragraph{Robustness.} \todo{The variance is not an artifact of one
perturbation type or one coder: openness/clamping holds across cache-breaking
mechanisms and across coding procedures.}
